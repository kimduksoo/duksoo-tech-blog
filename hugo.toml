baseURL = 'https://duksoo-tech-blog.pages.dev/'
languageCode = 'ko-kr'
title = "duksoo.dev"
theme = 'hugo-geekdoc'

# Geekdoc 설정
[params]
  description = "클라우드 인프라와 SRE, AI 에이전트 운영 기록. 일하면서 겪은 이슈와 해결 과정을 남깁니다."
  geekdocLogo = "logo.svg"

  # 검색 활성화
  geekdocSearch = true
  geekdocSearchShowParent = true

  # 다크모드
  geekdocDarkModeDim = false
  geekdocDarkModeCode = true

  # 네비게이션
  geekdocCollapseSection = true
  geekdocToC = 3

  # 기타
  geekdocRepo = "https://github.com/duksoo-son"
  geekdocEditPath = ""

[markup]
  [markup.highlight]
    anchorLineNos = false
    codeFences = true
    guessSyntax = false
    lineNos = true
    style = "monokai"
  [markup.tableOfContents]
    endLevel = 3
    startLevel = 1
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
