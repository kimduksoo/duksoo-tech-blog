# 블로그 배포

변경사항을 GitHub에 푸시하여 Cloudflare Pages에 자동 배포한다.

## 실행 내용

1. `git add -A`
2. `git commit -m "$ARGUMENTS"` (커밋 메시지)
3. `git push`
4. 배포 URL 안내: https://duksoo-tech-blog.pages.dev

## 사용법

`/publish 새 글 추가` → 커밋 메시지 "새 글 추가"로 푸시
